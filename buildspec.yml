version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file template.yaml --s3-bucket test-ci-scratch
                                   --output-template-file deploy/template.yaml
artifacts:
  type: zip
  files:
    - deploy/template.yaml
